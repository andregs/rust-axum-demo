# this kustomization inherits from our defaults
resources:
- ../default

# and it patches the configmap generated from 'default' to set the active profile
configMapGenerator:
- name: app-config
  behavior: merge
  literals:
  - app_profile=cloud

# and it patches other k8s resources too
patchesStrategicMerge:
- axum-demo-deployment.yaml
